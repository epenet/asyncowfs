name: pre-commit

on:
  pull_request:
  push:
    branches: [test-ci]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v3
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip anyio attr moat-util outcome pytest-trio trio
      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1
